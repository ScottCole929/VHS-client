import { useEffect } from "react"

const MyReviews = ({ userId }) => {
    const [userReviews, setUserReviews] = useState([])

    useEffect

}


return (
    <div>
        {userReviews.map((user_review)) => (
            <div key={userReviews.id}>
                <h3>{userReviews.</h3>
            </div>
        )}
    </div>
)